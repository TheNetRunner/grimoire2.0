import { CharReplacementPipe } from './char-replacement.pipe';

describe('CharReplacementPipe', () => {
  it('create an instance', () => {
    const pipe = new CharReplacementPipe();
    expect(pipe).toBeTruthy();
  });
});
