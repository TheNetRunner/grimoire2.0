import { StringAbbreviationerPipe } from './string-abbreviationer.pipe';

describe('StringAbbreviationerPipe', () => {
  it('create an instance', () => {
    const pipe = new StringAbbreviationerPipe();
    expect(pipe).toBeTruthy();
  });
});
